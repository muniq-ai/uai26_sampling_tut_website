# UAI 2026 Tutorial: Scalable Sampling of Bayesian Neural Networks

## Setup for the interactive notebooks

Use the provided `uv.lock` file to create a virtual environment with all the necessary dependencies. You can do this by:

1. Install `uv` (usually globally)
2. Create a virtual environment and install dependencies (`uv sync`)
